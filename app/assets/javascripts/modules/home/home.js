modulejs.define('', function() {
	return function() {
		// Vars
		// allGames = [];

		// promise = games.execute();			
		// promise.done(function(result) {
		// 	allGames = result.response;
		// 	console.log(allGames);
		// });
	}
});