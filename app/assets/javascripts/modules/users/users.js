modulejs.define('users', ['userForm'], 
	function(userForm) {
	return function() {		
		// jQuery Exec
		$(function() {
			// Vars
			// var $user = $('#form_user');
			
			// Form to open and close of Key Master					
			//userForm.openCloseKeyMaster();

		});
	}

});
